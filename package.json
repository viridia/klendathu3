{
  "devDependencies": {
    "lerna": "^2.9.0",
    "tslint": "^5.9.1",
    "tslint-react": "^3.5.1"
  },
  "dependencies": {
    "typescript": "^2.7.2"
  },
  "name": "klendathu",
  "version": "0.0.1",
  "description": "Flexible bug-tracking and project management system.",
  "main": "index.js",
  "scripts": {
    "client": "cd packages/klendathu-client && npm start",
    "server-up": "docker-compose -f docker-compose.yaml -f docker-compose.dev.yaml up --build server",
    "server-down": "docker-compose -f docker-compose.yaml -f docker-compose.dev.yaml down server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Talin",
  "license": "Apache-2.0"
}
