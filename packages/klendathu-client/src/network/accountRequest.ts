import { Account as AccountData } from 'klendathu-json-types';
// import { request } from '../models';

export function createUserAccount(email: string, password: string): Promise<AccountData> {
  return null;
}
